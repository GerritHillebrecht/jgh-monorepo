import { ShinyCardsDirective } from './shiny-cards.directive';

describe('ShinyCardsDirective', () => {
  it('should create an instance', () => {
    const directive = new ShinyCardsDirective();
    expect(directive).toBeTruthy();
  });
});
