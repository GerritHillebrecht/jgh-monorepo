import { GlowingBackgroundDirective } from './glowing-background.directive';

describe('GlowingBackgroundDirective', () => {
  it('should create an instance', () => {
    const directive = new GlowingBackgroundDirective();
    expect(directive).toBeTruthy();
  });
});
