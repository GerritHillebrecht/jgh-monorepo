import { BackgroundGlowCircleDirective } from './background-glow-circle.directive';

describe('BackgroundGlowCircleDirective', () => {
  it('should create an instance', () => {
    const directive = new BackgroundGlowCircleDirective();
    expect(directive).toBeTruthy();
  });
});
